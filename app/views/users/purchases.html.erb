<h2><%= @user.username%>'s Purchases</h2>
<p>Member since <%= @user.created_at.strftime("%Y-%m-%d") %></p>

<% if @user.list_purchases.empty? %>
  <%= "#{@user.username} is yet to purchase something..." %>
<% else %>
  <% @user.list_purchases.each do |order|%>
    <% order.order_items do |order_item| %>
      <%= order_item.product %>
    <% end %>
  <% end %>
  <hr>
<% end %>