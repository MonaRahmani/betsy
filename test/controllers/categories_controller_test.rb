require "test_helper"

describe CategoriesController do
  # describe "guest user" do
  #   before do
  #     @category = Category.first
  #   end
  #
  #   it "can(guest) can access product by category" do
  #     get category_products_path(@category.id)
  #     must_respond_with :success
  #   end
  # end
  #
  # describe "create" do
  #   before do
  #     @user = perform_login()
  #   end
  # end
  #
  # let(:category_hash) {
  #   {
  #       category: {
  #           category_name: "test category_name",
  #       }
  #   }
  # }
  # it "can create a category" do
  #   expect {
  #     post categories_path, params: category_hash
  #   }.must_differ "category.count", 1
  #
  #   must_respond_with :redirect
  #   must_redirect_to root_path
  #   expect(category.last.name).must_equal category_hash[:category][:category_name]
  # end
  #
  # it "won't create a category with invalid params" do
  #   category_hash[:category][:category_name] = nil
  #   expect {
  #     post categories_path, params: category_hash
  #   }.must_differ "category.count", 0
  # end
  #
  # describe "new" do
  #   it "can get the new category path" do
  #     get new_category_path
  #     must_respond_with :success
  #   end
  # end
  #
end
